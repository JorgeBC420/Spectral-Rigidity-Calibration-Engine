# Snapshot `files.zip` (2026)

Contenido extraído de `files.zip` del usuario:

- `ensemble_classifier.py`
- `rigidity.py`
- `r_statistic.py`
- `unfolding.py`

**No sustituyen** los módulos en `src/riemann_spectral/`. Se conservan aquí solo para **comparación** o fusión manual auditada. El código activo del motor SRCE sigue en `src/`.
